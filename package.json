{
  "name": "angle",
  "version": "1.0.0",
  "description": "A simplest protractor app for mac.",
  "author": "hrqsn",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "@nuxtjs/style-resources": "^1.0.0",
    "node-sass": "^4.14.1",
    "nuxt": "^2.0.0",
    "push-dir": "^0.4.1",
    "sass-loader": "^8.0.2"
  },
  "devDependencies": {}
}
